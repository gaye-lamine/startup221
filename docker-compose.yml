version: '3.8'

services:
  startup221_backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: startup221_backend
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "8088:8000"
